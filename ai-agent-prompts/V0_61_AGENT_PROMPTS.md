# v0.61 Agent Prompt — Easy Test Flow UX Pass

Use v0.60 as base and ship v0.61.

Focus: make testing obvious after loading a model. The Run Model page should show **Test this model** directly, with an editable prompt, starter prompts, and a readable response. Do not add dashboard clutter or turn the product into a generic chat UI. Preserve exact tested-run copy gating and keep Qwen3.6 / large Qwen text-generation models runnable.

Return `vllm-control-center-starter-v0.61.zip`.
