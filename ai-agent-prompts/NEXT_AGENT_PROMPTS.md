# Next Agent Prompt — v0.63 Beta Feedback Fix Pass

Use the v0.62 package as base and produce v0.63.

Choose the next slice from real tester feedback only. Good candidates:

- fresh install and bootstrap clarity
- first successful local vLLM launch on a real GPU box
- first successful remote GPU connection
- model-library clarity if testers still cannot find/load their local models

Do not add dashboard clutter. Do not turn the app into a generic chat UI. Keep Daily mode calm: Pick model -> Start -> Test this model -> Copy `/v1` base URL.

Return `vllm-control-center-starter-v0.63.zip`.
